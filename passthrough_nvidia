1. Run "virsh edit <VM_Name>"
2. Add the following below <features> (the value can be anything, doesn't matter):
<hyperv>
  <vendor_id state='on' value='whatever'/>
</hyperv>
<kvm>
  <hidden state='on'/>
</kvm>
3. Restart VM and let nvidia's drivers install!
